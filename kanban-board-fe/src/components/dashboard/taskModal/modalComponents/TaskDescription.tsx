import type { Task } from "../../../../types/models";

interface TaskDescriptionProps {
  userPermissions: number;
  task: Task;
  disabled: boolean;
}

const TaskDescription = (_props: TaskDescriptionProps) => {
  return null;
};

export default TaskDescription;
