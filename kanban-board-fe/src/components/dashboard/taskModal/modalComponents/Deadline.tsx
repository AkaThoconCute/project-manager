import type { Task } from "../../../../types/models";

interface DeadlineProps {
  task: Task;
  disabled: boolean;
}

const Deadline = (_props: DeadlineProps) => {
  return null;
};

export default Deadline;
