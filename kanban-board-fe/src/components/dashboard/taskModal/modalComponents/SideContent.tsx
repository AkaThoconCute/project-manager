import type { Task } from "../../../../types/models";

interface SideContentProps {
  task: Task;
}

const SideContent = (_props: SideContentProps) => {
  return null;
};

export default SideContent;
